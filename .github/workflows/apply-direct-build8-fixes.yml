name: Disabled direct Build 8 corrections

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Direct correction workflow retired
        run: echo "The verified source corrections have been committed. This one-time workflow is disabled."
