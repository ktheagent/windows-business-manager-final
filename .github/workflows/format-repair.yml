name: Disabled Build 8 repair workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Repair automation disabled
        run: echo "Automatic source repair is disabled. Source fixes must be committed directly."
